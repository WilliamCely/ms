spring:
  application:
    name: gateway
  profiles:
    default: oauth2

server:
  port: 4040

eureka:
  client:
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
